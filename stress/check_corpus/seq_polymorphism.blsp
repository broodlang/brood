;; sequence fns over lists, vectors, ranges, maps — all valid, no warnings.
(io/puts (map (list 1 2) inc) (map [1 2] inc) (map (range 3) inc))
(io/puts (fold [1 2 3] 0 +) (count {:a 1}) (first (keys {:a 1})))
(io/puts (filter [1 "a" 2 nil 3.5] int?))
(io/puts (sort-by count ["ccc" "a" "bb"]))
