;; redefinable globals are dynamic(): a def changing a global's type must not
;; make earlier or later uses warn (the reload contract, ADR-123..126).
(def cfg 42)
(defn show () (str cfg))
(io/puts (show))
(def cfg "now a string")
(io/puts (show))
(def cfg {:a 1})
(io/puts (get cfg :a))
