;; In a subdirectory — proves `brood test` discovers tests/**/*_test.blsp
;; recursively, not just at the top level. Register-only (no run-tests call).

(defmodule discovery-test (:use test))

(describe "discovery" (test "a nested test file is found and run" (assert= (* 6 7) 42)))
