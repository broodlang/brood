(defmodule doc-catalog
  "The Brood function catalogue: every public builtin and prelude function mapped to a
  functional category (math, strings, filesystem, processes, and so on), plus the category
  order and display titles. Shared by nest docs --all and hive's hosted /reference so both
  present the language reference identically. Regenerate when the builtin/prelude surface
  changes; a name absent from the catalog categorises as :other.")

(def *category-order*
  [[:math "Math and numbers"]
    [:strings "Strings and text"]
    [:collections "Sequences and collections"]
    [:maps "Maps and records"]
    [:sets "Sets"]
    [:bytes "Bytes and binary"]
    [:predicates "Predicates"]
    [:convert "Types and conversion"]
    [:control "Control flow and macros"]
    [:patterns "Pattern matching"]
    [:functions "Functions and combinators"]
    [:io "Input and output"]
    [:filesystem "Filesystem"]
    [:processes "Processes and concurrency"]
    [:errors "Errors"]
    [:meta "Modules and reflection"]
    [:system "System and environment"]
    [:other "Other"]])

(def *catalog*
  {"*" :math
   "*err*" :io
   "*features*" :meta
   "*load-path*" :meta
   "*print-length*" :io
   "*print-level*" :io
   "*print-string-length*" :io
   "*protocols*" :meta
   "*reload-diagnostics*" :meta
   "*require-parent*" :meta
   "*spy-sink*" :io
   "*out*" :io
   "*show*" :meta
   "+" :math
   "-" :math
   "->" :control
   "->>" :control
   "->decimal" :convert
   "->float" :convert
   "-conj" :meta
   "/" :math
   "<" :math
   "<=" :math
   "=" :math
   ">" :math
   ">=" :math
   "ability-id-kw" :meta
   "ability-qualify-op" :meta
   "add-load-path!" :meta
   "and" :control
   "any?" :predicates
   "append" :collections
   "append-empty?" :predicates
   "append-onto" :collections
   "apply" :functions
   "apropos" :meta
   "arglist" :meta
   "as->" :control
   "assoc" :maps
   "assoc-in" :maps
   "assoc-map-pairs" :maps
   "assoc-vec-pairs" :maps
   "binding" :meta
   "binding-names" :meta
   "binding-vals" :meta
   "bit-and" :math
   "bit-count" :math
   "bit-not" :math
   "bit-or" :math
   "bit-positions" :math
   "bit-shift-left" :math
   "bit-shift-right" :math
   "bit-xor" :math
   "bits->float" :convert
   "string/blank?" :predicates
   "bool?" :predicates
   "bound?" :meta
   "brood-version" :meta
   "build-id" :meta
   "builtin-modules" :meta
   "bytes" :bytes
   "bytes?" :predicates
   "string/capitalize" :strings
   "case" :control
   "case-check-test" :control
   "case-clauses" :control
   "chain?" :predicates
   "char->int" :strings
   "string/char-at" :strings
   "check-allow" :meta
   "cmp-ge" :math
   "cmp-gt" :math
   "cmp-le" :math
   "string/codepoints->" :strings
   "comment" :control
   "comp" :functions
   "comp-compose" :functions
   "compare" :math
   "compare-to" :meta
   "complement" :functions
   "cond" :control
   "cond->" :control
   "cond->>" :control
   "cond-orphan" :control
   "cond-thread-build" :control
   "conj" :collections
   "conj-one" :collections
   "cons" :collections
   "constantly" :functions
   "contains?" :predicates
   "contract-check-args" :meta
   "contract-check-rest" :meta
   "file/cp" :filesystem
   "count" :collections
   "count-acc" :collections
   "current-ns" :meta
   "file/cwd" :filesystem
   "dec" :math
   "decimal" :convert
   "decimal->float" :convert
   "decimal->string" :convert
   "decimal?" :predicates
   "def-" :control
   "defability" :meta
   "defability-derive-fn" :meta
   "defability-op-body" :meta
   "defability-op-call" :meta
   "defability-requires-vec" :meta
   "defability-sealed-vec" :meta
   "defdyn" :meta
   "defmacro" :control
   "defmethod" :meta
   "defmodule" :meta
   "defmodule-alias-clause" :meta
   "defmodule-alias-forms" :meta
   "defmodule-check-clause" :meta
   "defmodule-internals-clause" :meta
   "defmodule-internals-forms" :meta
   "defmodule-last-segment" :meta
   "defmodule-provide" :meta
   "defmodule-use-clause" :meta
   "defmodule-use-forms" :meta
   "defmulti" :meta
   "defn" :control
   "defn-" :control
   "defonce" :control
   "defrecord" :maps
   "defrecord-acc-sym" :maps
   "defrecord-derives" :maps
   "defrecord-fname" :maps
   "defrecord-ftype" :maps
   "defrecord-qualify" :maps
   "file/rmdir" :filesystem
   "file/rm" :filesystem
   "demonitor" :processes
   "denominator" :math
   "deprecation-warning" :meta
   "file/dir?" :filesystem
   "disj" :collections
   "display-width" :strings
   "dissoc" :maps
   "distinct-step" :collections
   "doc" :meta
   "doc-search" :meta
   "doc-search-terms" :meta
   "dolist" :control
   "dolist-loop" :control
   "doseq" :control
   "dotimes" :control
   "dotimes-loop" :control
   "doto" :control
   "drop" :collections
   "dynamic?" :meta
   "empty?" :predicates
   "string/ends-with?" :predicates
   "error" :errors
   "error-message" :errors
   "eval" :meta
   "eval-string" :meta
   "every?" :predicates
   "exit" :processes
   "feature?" :predicates
   "features" :meta
   "fields" :maps
   "file/exists?" :filesystem
   "file/mtime" :filesystem
   "file/size" :filesystem
   "file/stat" :filesystem
   "filter" :collections
   "first" :collections
   "flatten-acc" :collections
   "float->bits" :convert
   "float?" :predicates
   "floor" :math
   "fn?" :predicates
   "fold" :collections
   "fold-loop" :collections
   "for" :control
   "for-check-binds" :control
   "for-fold" :control
   "format-loop" :strings
   "format-prec" :strings
   "format-spec" :strings
   "gensym" :meta
   "get" :maps
   "get-in" :maps
   "get-in-walk" :maps
   "get-unanswerable" :maps
   "global-names" :meta
   "string/grapheme-at" :strings
   "string/grapheme-count" :strings
   "has-at-least?" :predicates
   "identity" :functions
   "if-let" :control
   "impl" :meta
   "impl-app?" :predicates
   "impl-fn-arity" :meta
   "impl-id-owner" :meta
   "impl-substrate?" :predicates
   "inc" :math
   "includes?" :predicates
   "index-of" :strings
   "index-of-seq-from" :strings
   "infinite?" :predicates
   "inspect" :io
   "int->char" :strings
   "int?" :predicates
   "into" :collections
   "iterate-times-acc" :collections
   "string/join" :strings
   "keys" :maps
   "keyword" :convert
   "keyword?" :predicates
   "last" :collections
   "last-index-of" :strings
   "link" :processes
   "list" :collections
   "string/list->" :strings
   "file/ls" :filesystem
   "list?" :predicates
   "load" :meta
   "string/lower" :strings
   "macroexpand" :meta
   "macroexpand-1" :meta
   "macroexpand-loop" :meta
   "file/mkdir" :filesystem
   "map" :collections
   "map-assoc" :maps
   "map-count" :maps
   "map-dissoc" :maps
   "map-get" :maps
   "map-int-add" :maps
   "map-pairs" :maps
   "map?" :predicates
   "match" :patterns
   "match*" :patterns
   "max" :math
   "merge" :maps
   "merge-acc" :collections
   "merge-runs" :collections
   "merge-sort-n" :collections
   "min" :math
   "mod" :math
   "module-doc" :meta
   "monitor" :processes
   "name" :strings
   "nan?" :predicates
   "nil?" :predicates
   "not" :predicates
   "not=" :math
   "nth" :collections
   "nth-bad-index" :collections
   "nth-seq-or-fail" :collections
   "num-add" :meta
   "num-div" :math
   "num-mul" :meta
   "num-sub" :meta
   "number?" :predicates
   "numerator" :math
   "or" :control
   "ord-compare" :meta
   "string/pad-left" :strings
   "string/pad-right" :strings
   "pair?" :predicates
   "partial" :functions
   "partition-acc" :collections
   "path-last-slash" :filesystem
   "pattern-vars" :patterns
   "pid?" :predicates
   "pr-bounded" :io
   "pr-bounded-map" :io
   "pr-bounded-seq" :io
   "pr-bounded-text" :io
   "pr-elide" :io
   "pr-limit" :io
   "pr-safely" :io
   "pr-str" :io
   "pr-str-bounded" :io
   "private?" :meta
   "provide" :meta
   "quot" :math
   "range" :collections
   "range?" :predicates
   "ratio?" :predicates
   "rational" :convert
   "read-all" :io
   "read-first" :io
   "read-line" :io
   "read-string" :io
   "receive" :processes
   "receive-binds" :processes
   "receive-body-code" :processes
   "receive-check-after" :processes
   "receive-clause-vars" :processes
   "receive-dedupe" :processes
   "receive-dispatch" :processes
   "receive-pin" :processes
   "receive-pinned-in" :processes
   "receive-prep" :processes
   "receive-result-clause" :processes
   "receive-split" :processes
   "receive-tags" :processes
   "record-id" :maps
   "record-register" :maps
   "record?" :predicates
   "reduce" :collections
   "ref" :processes
   "ref?" :predicates
   "register" :processes
   "register-impl-check-arity" :meta
   "register-method-derive" :meta
   "register-method-store" :meta
   "reload-defs" :meta
   "rem" :math
   "file/rename" :filesystem
   "repeat-acc" :collections
   "repeatedly-acc" :collections
   "string/replace" :strings
   "require-await" :meta
   "require-find" :meta
   "require-force" :meta
   "require-force-package" :meta
   "require-one" :meta
   "rest" :collections
   "reverse" :collections
   "reverse-acc" :collections
   "rope?" :predicates
   "run!" :control
   "satisfies?" :meta
   "second" :collections
   "self" :processes
   "send" :processes
   "seq" :collections
   "seqview?" :predicates
   "set-load-path!" :meta
   "set?" :predicates
   "show-args" :io
   "show-print-hook" :meta
   "sig" :meta
   "sig!" :meta
   "sig-pos" :meta
   "sleep" :processes
   "file/slurp" :filesystem
   "file/slurp-bytes" :filesystem
   "some->" :control
   "some->>" :control
   "some-thread-build" :control
   "sort" :collections
   "spawn" :processes
   "spawn-body-thunk" :processes
   "spawn-link" :processes
   "spawn-thunk-form?" :predicates
   "special-forms" :meta
   "file/spit" :filesystem
   "file/spit-append" :filesystem
   "file/spit-bytes" :filesystem
   "file/spit-private" :filesystem
   "split-at-acc" :collections
   "spy" :meta
   "spy-default-sink" :meta
   "spy-emit" :meta
   "spy-indent" :meta
   "spy-node" :meta
   "spy-run" :meta
   "spy-walk" :meta
   "spy-walk-binds" :meta
   "spy-walk-call" :meta
   "spy-walk-if" :meta
   "spy-walk-let" :meta
   "spy-walk-seq" :meta
   "string/starts-with?" :predicates
   "str" :strings
   "string/->codepoints" :strings
   "string/->graphemes" :strings
   "string/->list" :strings
   "string/length" :strings
   "string/normalize" :strings
   "string/repeat" :strings
   "string/span" :strings
   "string/span-until" :strings
   "string/split" :strings
   "string?" :predicates
   "string/substring" :strings
   "string/substring-graphemes" :strings
   "symbol" :convert
   "symbol?" :predicates
   "system-monitor" :meta
   "table?" :predicates
   "take" :collections
   "take-acc" :collections
   "take-while-acc" :collections
   "throw" :errors
   "->fixed" :strings
   "->seq" :meta
   "->string" :meta
   "trap-exit" :processes
   "string/trim" :strings
   "string/triml" :strings
   "string/trimr" :strings
   "try" :errors
   "try-class-catch?" :predicates
   "type-matches?" :predicates
   "type-of" :convert
   "unimpl-do" :meta
   "unless" :control
   "unlink" :processes
   "update" :maps
   "update-in" :maps
   "string/upper" :strings
   "vals" :maps
   "vec" :convert
   "vector" :collections
   "vector?" :predicates
   "when" :control
   "when-let" :control
   "whereis" :processes
   "with" :control
   "with-build" :control
   "with-err-str" :io
   "with-out-str" :io
   "with-split-else" :control
   "zero?" :predicates
   "zip-acc" :collections
   "hash-map" :maps
   "dissoc-in" :maps
   "zipmap" :maps
   "stdlib-id" :meta
   ;; ---- back in the prelude: used at macro-expansion time, so they cannot live in seq/
   "but-last" :collections
   "drop-while" :collections
   "mapcat" :collections
   "mapv" :collections
   "partition" :collections
   "repeat" :collections
   "sort-by" :collections
   "take-while" :collections
   "zip" :collections
   ;; ---- added so nothing falls into :other (every core name has a home) ----
   ;; (`gen`'s client API — call / cast / stop / spawn-server / defprocess — is NOT
   ;; here: `gen` is an ordinary module, so those names are `gen/…` and catalogued
   ;; with the module, exactly like `supervisor`'s and `agent`'s.)
   "defbehaviour" :meta
   "multimap?" :predicates
   "offload" :processes
   "ops" :meta
   "pq?" :predicates
   "queue?" :predicates
   "register-protocol" :meta
   "reserved-package-name?" :meta
   "tap" :control
   "then" :control})

(defn category-of (function-name)
  "The category keyword for a bare function name, or :other if uncatalogued."
  (get *catalog* function-name :other))
